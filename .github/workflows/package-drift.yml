name: Package Drift

on:
  schedule:
    - cron: "17 6 * * 1"
  workflow_dispatch:

jobs:
  package-drift:
    uses: decent-tools-for-thought/.github/.github/workflows/python-package-drift.yml@main
    with:
      package_import_name: uniprot_cli
      cli_command: uniprot
      run_tests: true
